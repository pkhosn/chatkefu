# Telegram Bot Token (必填)
# 从 @BotFather 获取
TG_BOT_TOKEN=your_bot_token_here

# 服务端口 (可选，默认 3000)
PORT=3000

# 会话过期天数 (可选，默认 7 天)
SESSION_EXPIRY_DAYS=7

# 最大文件上传大小 MB (可选，默认 20MB)
MAX_FILE_SIZE_MB=20
